source 'https://github.com/CocoaPods/Specs.git'  # cocopod的podSpec 管理路径
   

# 此处是管理cocoapod第三方库的显示状态 加上这两句之后 看起来更有层次了
install! 'cocoapods', :generate_multiple_pod_projects => true

platform :ios, '11.0'

target 'TBMtimeProject' do


   

end
